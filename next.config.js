/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,  // Garante que as URLs tenham a barra no final
    output: 'export',     // Adiciona configuração para exportação estática
  };
  
  module.exports = nextConfig;
  

